#include <iostream>
int main()
{
  int n;
  std::cin >> n;
  std::cout << n;
  return 0;
}



/*
Conventions:
- Identifiers cannot be given the name of a keyword
- Identifiers can consist of only letters, digits, and underscore
- Identifiers can only start with a letter or an underscore (not digits).
- It is a general convention to name methods & functions with camelCase
starting with lower case. e.g. getMarks(), setAge()
- The braces enclosing the body of the function should be aligned
with the functinon name.
- Within the braces, each statement should start from a distance
of one tab from the alignment of the braces.
 */
